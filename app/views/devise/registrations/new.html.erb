<div class="sign-up-form container">
<h2>Criar Conta</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :first_name,
                required: true,
                autofocus: true,  
                label: 'Nome' %>
    <%= f.input :last_name,
                required: true,  
                label: 'Sobrenome'%>
    <%= f.input :gender,
                required: true,
                collection: ["Masculino", "Feminino", "Outro"],
                label: 'Gênero'%>
    <%= f.input :phone,
                required: true,  
                label: 'Telefone'%> 
    <%= f.input :email,
                required: true,
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                required: true,
                hint: ("#{@minimum_password_length} characteres minimo" if @minimum_password_length),
                label: 'Senha' %>
    <%= f.input :password_confirmation,
                required: true,
                label: 'Confirmação de Senha'%>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Se Registrar", class: "btn btn-outline-warning btn-sm btn-block" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>